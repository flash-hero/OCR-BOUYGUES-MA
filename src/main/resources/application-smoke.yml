# Profil "smoke" : lance le test decisif Mistral OCR sans demarrer de serveur web.
spring:
  main:
    web-application-type: none
    banner-mode: "off"

logging:
  level:
    root: WARN
    com.bycn.edoc: INFO
